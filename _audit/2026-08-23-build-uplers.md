# Uplers build - the leak walker, and his paid agent's output

**ITEM ZERO: uplers did NOT have naukri's defect, and had three others.** The sweep's `SECRETS` holds the JWT strings themselves, so a whole-token echo always fired. Measured blind instead: (1) one base64url **segment** alone - `token.split(".")[1]` is not a superstring of the token, yet it decodes to the entire identity half; (2) **decoded claims**, which is not hypothetical because `token_expiry` already decodes that segment - that one IS naukri's failure transposed; (3) the `secret[:12]` clause was matching a **constant** - `eyJhbGciOiJI`, the standard HS256 header, identical across all three JWTs, so zero credential-specific signal plus three false fires on prose describing a JWT. And the only control planted a **Sanctum** token, a format that wears its secret in the clear, so the JWT arm was certified by nothing. Replaced with a per-credential fragment set (whole value, each segment, decoded claims, sanctum tail) minus the fragments of a **format decoy**, so the header is subtracted by measurement rather than by hand. **Reverting to the old rule turns 4 of the 5 new controls red**; the plaintext and whole-JWT controls stay green, which is exactly the point. Same detector now backs the JWT arm in `test_session.py` and `test_talent_tools.py`.

**THE AGENT READ-THROUGH IS THE HEADLINE, AND IT IS WORSE THAN "INVISIBLE".** His paid agent has run **48 jobs and produced 8 POSITIVE REPLIES - 7 of them unseen**. Real people at real companies wrote back and nobody answered: "willing to refer, requests updated resume" has waited **12 days**; six more behind it ("forwarding profile to hiring manager", "referral submitted", "shared resume with HR"). The parity audit read his state as *9 applications, 8 stuck at "Added", the bottleneck is conversion* - this is the conversion, and it was sitting in a route we did not call. Second finding, free: `linkedin_connected: false` / `linkedin_template: false` while gmail is both, and **Uplers' own failure text names it on 11 of the 16 failed runs**. His two-channel agent runs on one, and it is costing him roughly a quarter of its attempts.

**Shipped:** `uplers_agent_readthrough`, `uplers_platform_saved_jobs`, `uplers_my_preferences`, `uplers_assessment_gates`. 43 -> **47 tools**, **1190 -> 1214 tests**. Four modules (`outreach`, `saved_filter`, `preference`, `assessment_flags`) built by three implementer slices, reviewed and integrated here; slice receipts in `_audit/_slices/`.

**Contracts pinned, each shown failing.** `is_saved_filter` is the integer `1` (test uses `type(x) is int` - `True == 1` in Python, so the naive assertion cannot fail) and is **exclusive**, so the builder REFUSES a dropped filter rather than returning a wrong answer that looks right. Two envelope idioms: `outreach-step` sends the STRING `"success"`, the other four the INTEGER `200`. Empty `data: []` is an answer, not a failure. `preference` could not reuse `talent_shape.masters_index` - it keys on `row["value"]`, but this payload's `cities` puts the id under `id`, so a value-keyed index resolves none of his cities silently. Assessment flags keep **absent** distinct from **false**. Where two Uplers routes disagree (`consent_email_job_scan` true vs `has_consent` false; `auto_run` 1 vs `auto_run_consent` false) both are printed with receipts.

**The paid-SKU boundary is MEASURED, not asserted.** Three of four tools read inside `talent/outreach/*`, one path segment from `consent-email-job-scan`. `tests/test_agent_tools.py` checks the requests that actually left: all GET, against an **exact route allowlist** (the dangerous mistake here is not a POST, it is a GET to the wrong sibling), with a control proving the census records a write when one happens. **Six mutations on server.py, six caught**; restored byte-identical. No autonomous applier built - he owns one, and Tier C stays empty.

**Correction to the brief: `find-similar-job` is not a read.** It is a **POST carrying his email address** in the body (`{hr_id, user_email}`, export `NuB`). Recorded in `endpoints.py`, not built: read-shaped is not read, it would put the first non-write POST into a server whose write-surface census is load-bearing, and all 250 requisitions are already indexed locally so "similar to this" is answerable offline with jobcore's comparable scoring. Not probed live either. `talent/get-preference` **is** a genuine read and is now VERIFIED LIVE, which beats the bundle constant - and the `fJ7`-is-nurture warning did not recur.

**SECURITY, needing his decision - and TWO CORRECTIONS AGAINST MYSELF, both found by a reviewer and both verified before being accepted.** My seven live captures were swept into **`7984a3f`** by a concurrent `git add -A` and pushed before redaction. `9a0481b` reported *"an independent secret sweep over all eight files found nothing sensitive"*; that is false - the blob still reads `current_ctc "1650000"`, `expected_ctc "2400000"`, and carries seven named third parties with business emails, LinkedIn URLs, his gmail, real Gmail thread ids and verbatim reply bodies.

**CORRECTION 1 - my count was wrong and read as a total.** I reported "12 times". That was `grep -c` over THREE hand-picked needles, counting LINES, and I presented it as the whole. Measured properly on the same blob: **37 lines carry an email-shaped string, 7 carry a LinkedIn profile URL - 44 - and 8 DISTINCT real addresses.** My figure undercounts by 3.7x. `tests/test_fixture_hygiene.py` now pins the per-field breakdown rather than one number, so a future re-count says which field moved.

**CORRECTION 2 - my DIAGNOSIS was wrong, and the true one is worse.** Commit `d1a9148` says "the scrub map was built AFTER the first capture run". It was not. `git show 7984a3f --name-status` shows `scripts/capture_outreach.py` added in the SAME commit as all seven fixtures: a redaction map existed and ran, and it failed three ways. (1) `DROP` tested exact key membership and held `email`, while the real keys are `to_email`, `from_email`, `employee_business_email`, `contact_value`, `contact_display` - none matched. (2) The pay keys were not in `DROP` at all. (3) **`SUSPICIOUS` DID see the pay keys and printed them, and the write was unconditional.** The detector fired and nothing consulted it. That is the lesson worth more than the scrub: an ADVISORY guard is not a guard, and six clean fixtures made a broken filter look like a working one when they were clean only because those routes carry no contact data.

**Bounded by measurement:** `gh repo view` gives **PRIVATE, forkCount 0**, one owner, one remote - no third party ever had read access. Working tree and HEAD are clean. History surgery on a shared branch with live CI is his call. Capture now deletes pay keys, masks contact values, and **re-reads off disk to prove it**, deleting the file on a hit rather than printing a flag nobody reads.

**THE TRANSFORM GRID - the clearance was wrong and so was my fix.** I was told to skip item zero because "uplers walks the whole assembled JWT strings plus a 12-character prefix"; that was retracted, and the retraction was right for a reason neither version stated: **the encoding does not have to live in the credential, it can live in the LEAK PATH.** `scripts/credential_echo_control.py` + `scripts/leak_matrix.py` (adapted from linkedin's, transform list kept identical so results compare) echo the test's OWN planted credential back out of eight guarded assertions under nine transforms. **72 cells, four runs:**

| run | green | what it found |
|---|---|---|
| 1 | **52/72** | Not honest. Four columns green even under `verbatim` - impossible for a real detector. The plugin hooked `SessionStore.token`, but `describe()` calls `self.read()` and never goes through `token()`, so those tests had **no credential to echo**. THE INSTRUMENT WAS WRONG BEFORE THE SUBJECT WAS. |
| 2 | **42/72** | The real baseline. `b64`, `b64url`, `hex` each caught by exactly ONE of eight assertions - and that one caught by STRUCTURE (an exact key-set assertion refusing an unknown key), not by value. **Every value-hunting assertion in the repo was blind to all three.** |
| 3 | **27/72** | Detector given the encoded spellings, 12-character RUNS rather than whole strings, and a `JWT_SHAPE` regex needing no canary - the only rule that could catch his REAL token down a path no test planted into. |
| 4 | **18/72** | The last two hand-rolled substring assertions routed through the detector. They were green under b64, b64url, hex, percent and split. |

**Every remaining green is correct.** Fifteen are the log row and log column - a caplog test cannot see a payload leak and payload tests cannot see a log line; each catches what the other cannot. Three are `prefix12` on the JWT columns, where the first twelve characters are `eyJhbGciOiJI`, the base64 of a standard HS256 header, identifying nothing; the SANCTUM columns ARE red there because `42|bearer-to` does identify a credential. That discrimination is the detector working. Controls are **generated from the adversary's `TRANSFORMS` tuple**, so a spelling with no decision about it fails immediately; the three exemptions each assert that firing would be WRONG, and the list is pinned at three.

Two things the grid taught that the fix would not have: the subtraction had to move to **run level** (removing whole shared fragments leaves every window inside them, so run-hunting brought the generic-header false positive straight back), and one of my own witnesses was invalid - `claims_segment[:12]` is `eyJzdWIiOiJ0`, which decodes to `{"sub":"t`, shared with the decoy and correctly subtracted. A witness chosen by position rather than uniqueness tests the decoy, not the rule.

**Gate: 1245 passed locally; CI `32618031825` SUCCESS on all four legs** (ubuntu/windows x 3.11/3.13) at `dc9c81e`. Pushed to `master`. No AI co-author trailer.
